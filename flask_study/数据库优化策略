1、最左原则
@建立索引后,查询时尽量将索引条件放在select的where条件前面
@可以建立联合索引
2、放弃外键
@大数据量时放弃外键,因为外键需要维护cacs级联删除的属性,影响数据库性能,手动补充索引
3、尽量减少不需要查询的字段
@db.session.query(User).all() X         db.session.query(User.id,User.name).all() √
4、单条sql语句  优于>  连表查询   优于>子查询
5、以空间换时间策略
@如果关联表的某一个字段经常会被查询,可以将他放入主表中做一个冗余字段,这样可以减少查询次数

